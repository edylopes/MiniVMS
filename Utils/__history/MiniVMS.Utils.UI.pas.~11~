unit MiniVMS.Utils.UI;

interface

uses
  Winapi.Windows,
  Vcl.Controls;

procedure SetRoundedCorners(
  AControl: TWinControl;
  ARadius: Integer = 12
);

implementation

procedure SetRoundedCorners(
  AControl: TWinControl;
  ARadius: Integer
);
var
  Region: HRGN;
begin
  if not Assigned(AControl) then
    Exit;

  Region := CreateRoundRectRgn(
    0,
    0,
    AControl.Width,
    AControl.Height,
    ARadius,
    ARadius
  );

  if SetWindowRgn(AControl.Handle, Region, True) = 0 then
    DeleteObject(Region);
end;

end.
